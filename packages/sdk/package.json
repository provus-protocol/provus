{
  "name": "@provus/sdk",
  "version": "0.1.0",
  "description": "Provus SDK — HTTP interfaces for runtime, operator, and query surfaces",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "node --watch dist/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@provus/core": "*",
    "fastify": "^4.28.1",
    "pino-pretty": "^13.1.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
