{
  "name": "provus",
  "version": "0.1.0",
  "private": true,
  "description": "Agent identity, attestation, and reputation protocol",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "demo": "npm run demo --workspace=examples/basic-flow"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
